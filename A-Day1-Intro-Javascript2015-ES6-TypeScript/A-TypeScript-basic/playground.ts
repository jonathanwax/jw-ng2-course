var name :string;

function hello(name: string){
  let a = name;
}

function helloNumber(name: number) : number{
    return name;
}

hello("string");
let result = helloNumber(10);

enum Color {Red, Green, Blue};
